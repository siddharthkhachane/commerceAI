services:
  postgres:
    image: postgres:16-alpine
    container_name: commerceai-postgres
    restart: unless-stopped
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-commerceai}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-commerceai}
      POSTGRES_DB: ${POSTGRES_DB:-commerceai}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-commerceai} -d ${POSTGRES_DB:-commerceai}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
